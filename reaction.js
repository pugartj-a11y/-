const box=document.getElementById('reaction'),txt=document.getElementById('reactionText'),res=document.getElementById('result');let waiting=false,startTime;function reset(){waiting=false;box.classList.remove('ready');txt.textContent='ЖДИ ГОЛУБОГО...';let delay=1200+Math.random()*2800;setTimeout(()=>{waiting=true;startTime=performance.now();box.classList.add('ready');txt.textContent='ЖМИ!'},delay)}box.addEventListener('click',()=>{if(!waiting){if(box.classList.contains('ready'))return;txt.textContent='Слишком рано! Жми, чтобы попробовать снова';setTimeout(reset,900);return}let ms=Math.round(performance.now()-startTime);res.textContent=ms;box.classList.remove('ready');txt.textContent='Результат: '+ms+' мс. Жми для нового раунда';waiting=false});box.addEventListener('click',()=>{if(txt.textContent.includes('нового раунда'))reset()});