const btn=document.getElementById('clickBtn');if(btn){let score=0,time=10,started=false,timer;btn.addEventListener('click',()=>{if(!started){started=true;score=0;time=10;document.getElementById('message').textContent='Поехали!';timer=setInterval(()=>{time--;document.getElementById('time').textContent=time;if(time<=0){clearInterval(timer);started=false;btn.disabled=true;document.getElementById('message').textContent='Готово! Результат: '+score+' кликов. Обнови страницу, чтобы сыграть снова.'}},1000)}if(started){score++;document.getElementById('score').textContent=score}})}